Flask
requests
firebase-admin  # If you are using firebase-admin in a more complex setup. 
gunicorn                # If you are only using the 'requests' library to talk to Firebase's REST API, you may not need this.